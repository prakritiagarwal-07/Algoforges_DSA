CREATE TABLE LEETCODE_PROBLEMS (
    PROB_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(255),
    SLUG VARCHAR2(255),
    DIFFICULTY VARCHAR2(50),
    RAW_JSON CLOB,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

